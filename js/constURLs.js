const baceURL = "http://localhost/projects/PFE/solicode/backend/api/";

const articleURL = baceURL + "article.php";
const commentURL = baceURL + "comment.php";
const problemURL = baceURL + "problem.php";
const communityURL = baceURL + "community.php";
const taskURL = baceURL + "task.php";
const loginURL = baceURL + "login.php";
const signupURL = baceURL + "signup.php";
const profileURL = baceURL + "profile.php";
const userTaskURL = baceURL + "userTask.php";
const createAutho = baceURL + "autho.php";
const getCatigory = baceURL + "getCatigory.php";
const forgetPwd = baceURL + "forgetPwd.php";
const dashboard = baceURL + "dashboard.php";

export {
  articleURL,
  commentURL,
  problemURL,
  communityURL,
  taskURL,
  loginURL,
  signupURL,
  profileURL,
  userTaskURL,
  createAutho,
  getCatigory,
  forgetPwd,
  dashboard,
};
